// Initialization for ES Users
import {
    Collapse,
    Dropdown,
    Tab,
    Ripple,
    initTWE,
} from "tw-elements";

import 'tw-elements';

initTWE({ Collapse, Dropdown });
initTWE({ Tab });
initTWE({ Ripple });

// Initialization for ES Users
